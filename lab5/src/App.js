import './App.css'; // I don't know if you read the comments, but i like keeping the css for App.js because dark mode lol
import Search from './components/search'; // imports the Search component

// the App function that makes everything run
function App() {
  return (
    // return the Search component
    <Search />
  );
}

export default App;
